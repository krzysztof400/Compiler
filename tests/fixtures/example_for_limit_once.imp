PROGRAM IS
  x, i
IN
  x := 1;

  # Limit should be computed once: x+2 == 3.
  # Even though x changes in the loop, the loop must still run i=1..3.
  FOR i FROM 1 TO x + 2 DO
    x := x + 1;
    WRITE i;
  ENDFOR
END
